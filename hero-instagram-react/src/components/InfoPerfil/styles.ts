import { CSSProperties } from 'react';

const photoPerfil: CSSProperties = {
  height: '150px',
  width: '150px',
  borderRadius: '100',
  margin: '20px',
};

const row: CSSProperties = {
  display: 'flex',
  flexDirection: 'row',
};

const info: CSSProperties = {
  display: 'flex',
  flexDirection: 'column',
  justifyContent: 'center',
};

export default {
  photoPerfil,
  row,
  info,
};
